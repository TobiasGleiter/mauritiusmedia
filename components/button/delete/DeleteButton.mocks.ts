import { IDeleteButton } from './DeleteButton';

const base: IDeleteButton = {
  primary: true,
  size: 'text-sm',
  label: 'Button',
  style: 'text-black',
  id: '123',
};

export const mockDeleteButtonProps = {
  base,
};
